-- ============================================================================
--  ترقية: تفاصيل نوع المشروع
--
--  عمود JSON يحفظ إجابات حقول النوع (عدد المنتجات، بوابة الدفع، اللغات…).
--  الحقول نفسها معرّفة في src/lib/project-types.ts، فإضافة حقل جديد لأي نوع
--  تعديل كود فقط بدون ترقية قاعدة بيانات.
--
--  التشغيل:  mysql -u root -p arqam_flow < db/migrations/003_type_details.sql
-- ============================================================================

SET NAMES utf8mb4;

ALTER TABLE projects
  ADD COLUMN type_details JSON NULL AFTER project_type;
