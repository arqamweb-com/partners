# حزمة الاختبار لا تُنفَّذ من المتصفح إطلاقًا — التيرمينال فقط.
# (api/.htaccess يمرّر الملفات الموجودة فعلًا كما هي، فبدون هذا السطر
#  كان /api/tests/setup.php ينفَّذ من المتصفح ويمسح جداول قاعدة الاختبار)
Require all denied
