<?php
/**
 * قالب إعدادات الاتصال.
 *
 * على السيرفر: انسخه باسم config.php في نفس المجلد، وغيّر الثلاثة أسطر
 * المعلَّمة بـ  ← غيّرها  ولا شيء غيرها.
 *
 * القيم هنا مضبوطة للاستضافة الحقيقية. للتشغيل المحلي على MAMP انظر
 * التعليقات بجانب كل قيمة.
 *
 * config.php لا يُرفع على git ولا يُنسخ في نسخة الاستضافة — يُنشأ يدويًا.
 */

return [
    // ---- قاعدة البيانات ----
    'db' => [
        // على cPanel دائمًا localhost. محليًا على MAMP: 127.0.0.1
        'host'    => 'localhost',

        // على cPanel دائمًا 3306. محليًا على MAMP: 8889
        'port'    => 3306,

        // الاسم الكامل بالبادئة، مثل: u123456_arqam_flow      ← غيّرها
        'name'    => 'DATABASE_NAME',

        // مستخدم قاعدة البيانات بالبادئة، مثل: u123456_arqam   ← غيّرها
        'user'    => 'DATABASE_USER',

        // كلمة مرور المستخدم                                   ← غيّرها
        'pass'    => 'DATABASE_PASSWORD',

        'charset' => 'utf8mb4',
    ],

    // ---- الجلسات ----
    'session' => [
        'name'     => 'arqam_session',
        'lifetime' => 60 * 60 * 12,   // 12 ساعة

        // true مع HTTPS (الوضع الصحيح على الاستضافة).
        // محليًا على MAMP بدون HTTPS: false وإلا لن يعمل تسجيل الدخول.
        'secure'   => true,
    ],

    // ---- عام ----
    // false على السيرفر: يمنع ظهور تفاصيل الأخطاء للزوّار.
    // true محليًا فقط أثناء التطوير.
    'debug' => false,
];
